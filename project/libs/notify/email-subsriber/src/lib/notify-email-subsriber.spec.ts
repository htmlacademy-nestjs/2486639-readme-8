import { notifyEmailSubsriber } from './notify-email-subsriber';

describe('notifyEmailSubsriber', () => {
  it('should work', () => {
    expect(notifyEmailSubsriber()).toEqual('notify-email-subsriber');
  });
});
