import { accountAuthentication } from './account-authentication';

describe('accountAuthentication', () => {
  it('should work', () => {
    expect(accountAuthentication()).toEqual('account-authentication');
  });
});
